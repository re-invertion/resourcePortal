import { useCallback, useEffect, useState } from "react";
import { apiRequest } from "../../api/client";
import { Button, Callout, Card, ConfirmActionButton, DataTable, EmptyState, Field, GlobeIcon, NetworkIcon, NumberInput, PlusIcon, Select, TextInput, TrashIcon } from "../../components/design-system";
import { idOf, items, text } from "../../hooks/use-api";

type R=Record<string,unknown>; type Endpoint={ app:R; endpoint:R };
export function AppGroupNetworking({tenantId,appGroupId}:{tenantId:string;appGroupId:string}){
 const root=`/api/tenants/${encodeURIComponent(tenantId)}/app-groups/${encodeURIComponent(appGroupId)}`; const [apps,setApps]=useState<R[]>([]); const [rows,setRows]=useState<Endpoint[]>([]); const [loading,setLoading]=useState(true); const [error,setError]=useState<string>(); const [open,setOpen]=useState(false); const [editing,setEditing]=useState<Endpoint>(); const [appId,setAppId]=useState(""); const [name,setName]=useState(""); const [port,setPort]=useState(8080); const [protocol,setProtocol]=useState("HTTP_REDIRECT_TO_HTTPS"); const [working,setWorking]=useState(false);
 const load=useCallback(async()=>{setLoading(true);setError(undefined);try{const appList=items<R>(await apiRequest(`${root}/single-apps`));setApps(appList);setAppId(current=>current||(appList[0]?idOf(appList[0]):""));const all=await Promise.all(appList.map(async app=>items<R>(await apiRequest(`${root}/single-apps/${encodeURIComponent(idOf(app))}/http-endpoints`)).map(endpoint=>({app,endpoint}))));setRows(all.flat());}catch(c){setError(c instanceof Error?c.message:"Networking could not be loaded.");}finally{setLoading(false);}},[root]);
 useEffect(()=>{void load();},[load]);
 function begin(row?:Endpoint){setEditing(row);setAppId(row?idOf(row.app):(apps[0]?idOf(apps[0]):""));setName(text(row?.endpoint.name,""));setPort(Number(row?.endpoint.containerPort??8080));setProtocol(text(row?.endpoint.protocolMode,"HTTP_REDIRECT_TO_HTTPS"));setOpen(true);}
 async function save(){if(!appId||!name||!port)return;setWorking(true);setError(undefined);try{const base=`${root}/single-apps/${encodeURIComponent(appId)}/http-endpoints`;const endpointId=editing?idOf(editing.endpoint):"";await apiRequest(`${base}${endpointId?`/${encodeURIComponent(endpointId)}`:""}`,{method:endpointId?"PATCH":"POST",body:{name,containerPort:port,protocolMode:protocol}});setOpen(false);setEditing(undefined);await load();}catch(c){setError(c instanceof Error?c.message:"Endpoint could not be saved.");}finally{setWorking(false);}}
 async function remove(row:Endpoint){try{await apiRequest(`${root}/single-apps/${encodeURIComponent(idOf(row.app))}/http-endpoints/${encodeURIComponent(idOf(row.endpoint))}`,{method:"DELETE"});await load();}catch(c){setError(c instanceof Error?c.message:"Endpoint could not be deleted.");}}
 return <section className="space-y-4"><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><h2 className="text-xl font-semibold">Networking</h2><p className="mt-1 text-sm text-[#5B6678]">HTTP endpoints and protocol behavior for applications in this App Group.</p></div><Button variant="primary" disabled={!apps.length} onClick={()=>begin()}><PlusIcon size={15}/>Add endpoint</Button></div>{error?<Callout tone="danger" title="Networking request failed">{error}</Callout>:null}{open?<Card className="grid gap-4 p-5 md:grid-cols-2"><Field label="Application" required><Select value={appId} onChange={e=>setAppId(e.target.value)} disabled={Boolean(editing)}>{apps.map(a=><option key={idOf(a)} value={idOf(a)}>{text(a.name,"Application")}</option>)}</Select></Field><Field label="Endpoint name" required><TextInput value={name} onChange={e=>setName(e.target.value)} placeholder="web"/></Field><Field label="Container port" required><NumberInput min={1} max={65535} value={port} onChange={e=>setPort(Number(e.target.value))}/></Field><Field label="Protocol mode"><Select value={protocol} onChange={e=>setProtocol(e.target.value)}><option value="HTTP">HTTP</option><option value="HTTPS">HTTPS</option><option value="HTTP_AND_HTTPS">HTTP + HTTPS</option><option value="HTTP_REDIRECT_TO_HTTPS">HTTP redirect to HTTPS</option></Select></Field><div className="flex gap-2 md:col-span-2"><Button variant="primary" disabled={working} onClick={()=>void save()}>{working?"Saving…":editing?"Save changes":"Create endpoint"}</Button><Button onClick={()=>setOpen(false)}>Cancel</Button></div></Card>:null}<Card className="overflow-hidden"><DataTable loading={loading} columns={[{key:"app",label:"Application"},{key:"endpoint",label:"Endpoint"},{key:"port",label:"Port"},{key:"protocol",label:"Protocol"},{key:"actions",label:""}]} rows={rows.map(r=>({key:`${idOf(r.app)}-${idOf(r.endpoint)}`,cells:{app:<span className="flex items-center gap-2"><NetworkIcon size={15}/><strong>{text(r.app.name)}</strong></span>,endpoint:text(r.endpoint.name),port:text(r.endpoint.containerPort),protocol:text(r.endpoint.protocolMode,"HTTP"),actions:<div className="flex justify-end gap-1"><Button size="sm" variant="ghost" onClick={()=>begin(r)}>Edit</Button><ConfirmActionButton size="sm" triggerVariant="ghost" ariaLabel={`Delete ${text(r.endpoint.name)}`} confirmTitle={`Delete endpoint ${text(r.endpoint.name)}?`} confirmDescription="This removes the HTTP endpoint and its routing entry from this application." confirmLabel="Delete endpoint" onConfirm={()=>remove(r)}><TrashIcon size={15}/></ConfirmActionButton></div>}}))} empty={<EmptyState icon={<GlobeIcon/>} title="No HTTP endpoints" description="Expose an application port when this workload needs HTTP routing."/>}/></Card><Card className="p-5"><div className="flex items-start gap-3"><GlobeIcon className="mt-0.5 text-[#1769E0]"/><div><h3 className="font-semibold">Domains & routing</h3><p className="mt-1 text-sm text-[#5B6678]">Tenant domains are managed centrally and can be used by routing policies supported by the platform.</p><a className="mt-2 inline-block text-sm font-semibold text-[#0F56A7] hover:underline" href={`/tenants/${encodeURIComponent(tenantId)}/domains`}>Manage tenant domains</a></div></div></Card></section>;
}

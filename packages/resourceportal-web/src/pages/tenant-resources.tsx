import { useEffect, useState } from "react";
import { apiRequest } from "../api/client";
import { ActivityIcon, BillingIcon, Button, Callout, Card, DataTable, EmptyState, Field, GlobeIcon, GridIcon, KeyIcon, LinkButton, NetworkIcon, NumberInput, PageHeader, RegistryIcon, ServerIcon, StatusBadge, StatusText, TextInput, UsersIcon, VolumeIcon, statusTone } from "../components/design-system";
import { formatBytes, formatDate, idOf, items, text, useApi } from "../hooks/use-api";
import { tenantHref } from "../router/router";

type R = Record<string, unknown>;

function ResourceSummary({ title, description, href, count, icon, loading, error }: { title:string; description:string; href:string; count:number; icon:React.ReactNode; loading:boolean; error?:unknown }) {
  return <a href={href} className="rounded-lg border border-[#D7E0EC] bg-white p-5 transition hover:border-[#AFC7E8] hover:shadow-[0_4px_16px_rgba(36,74,120,.06)]"><div className="flex items-start gap-3"><span className="flex h-10 w-10 items-center justify-center rounded-lg bg-[#E7F1FF] text-[#1769E0]">{icon}</span><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-3"><h2 className="font-semibold">{title}</h2><strong className="text-lg">{loading?"—":error?"!":count}</strong></div><p className="mt-1 text-sm leading-5 text-[#5B6678]">{description}</p></div></div></a>;
}

export function TenantStorageNetworking({tenantId}:{tenantId:string}) {
  const root=`/api/tenants/${encodeURIComponent(tenantId)}`; const volumes=useApi<unknown>(`${root}/volumes`); const registries=useApi<unknown>(`${root}/registries`); const domains=useApi<unknown>(`${root}/domains`);
  const volumeRows=items<R>(volumes.data); const registryRows=items<R>(registries.data); const domainRows=items<R>(domains.data);
  return <main><PageHeader eyebrow="Tenant resources" title="Storage & Networking" description="Volumes, container registries, domains and HTTP routing for this tenant."/><div className="grid gap-4 md:grid-cols-3"><ResourceSummary title="Volumes" description="Persistent storage allocated to application workloads." href={tenantHref(tenantId,"volumes")} count={volumeRows.length} icon={<VolumeIcon/>} loading={volumes.loading} error={volumes.error}/><ResourceSummary title="Registries" description="Private and public container image sources." href={tenantHref(tenantId,"registries")} count={registryRows.length} icon={<RegistryIcon/>} loading={registries.loading} error={registries.error}/><ResourceSummary title="Domains" description="Managed hostnames, TLS and HTTP routing." href={tenantHref(tenantId,"domains")} count={domainRows.length} icon={<GlobeIcon/>} loading={domains.loading} error={domains.error}/></div><div className="mt-6 grid gap-6 xl:grid-cols-2"><Card className="overflow-hidden"><div className="flex items-center justify-between border-b border-[#E1E7F0] px-5 py-4"><h2 className="font-semibold">Volumes</h2><LinkButton className="h-8 px-3 text-xs" href={tenantHref(tenantId,"volumes")}>Manage</LinkButton></div><DataTable loading={volumes.loading} columns={[{key:"name",label:"Volume"},{key:"size",label:"Size"},{key:"usage",label:"Used"}]} rows={volumeRows.slice(0,6).map(v=>({key:idOf(v),cells:{name:<strong>{text(v.name)}</strong>,size:formatBytes(v.sizeBytes),usage:formatBytes(v.usedSizeBytes)}}))} empty={<EmptyState icon={<VolumeIcon/>} title="No volumes" description="Create a persistent volume when an application needs durable storage."/>}/></Card><Card className="overflow-hidden"><div className="flex items-center justify-between border-b border-[#E1E7F0] px-5 py-4"><h2 className="font-semibold">Domains & registries</h2><LinkButton className="h-8 px-3 text-xs" href={tenantHref(tenantId,"domains")}>Routing</LinkButton></div><div className="divide-y divide-[#E1E7F0]">{domainRows.slice(0,4).map(d=><div key={idOf(d)} className="flex items-center justify-between gap-3 px-5 py-3"><div className="min-w-0"><strong className="block truncate text-[13px]">{text(d.hostname,text(d.fqdn,text(d.prefix,"Domain")))}</strong><span className="text-xs text-[#718096]">{text(d.type,"Managed")}</span></div><StatusText tone={statusTone(d.status??d.verificationStatus)}>{text(d.status,text(d.verificationStatus,"Configured"))}</StatusText></div>)}{!domainRows.length&&!domains.loading?<div className="p-5"><EmptyState icon={<GlobeIcon/>} title="No domains" description="Add a domain to route HTTP traffic to applications."/></div>:null}<div className="flex flex-wrap gap-2 px-5 py-4"><LinkButton className="h-8 px-3 text-xs" href={tenantHref(tenantId,"registries")}>{registryRows.length} registries</LinkButton><LinkButton className="h-8 px-3 text-xs" href={tenantHref(tenantId,"domains")}>{domainRows.length} domains</LinkButton></div></div></Card></div></main>;
}

export function TenantAccess({tenantId}:{tenantId:string}) {
  const root=`/api/tenants/${encodeURIComponent(tenantId)}`; const memberships=useApi<unknown>(`${root}/memberships`); const invitations=useApi<unknown>(`${root}/invitations`); const roles=useApi<unknown>(`${root}/roles`); const groups=useApi<unknown>(`${root}/groups`); const idps=useApi<unknown>(`${root}/identity-providers`); const oauth=useApi<unknown>(`${root}/oauth-applications`); const identities=useApi<unknown>(`${root}/service-identities`);
  const memberRows=items<R>(memberships.data); const inviteRows=items<R>(invitations.data); const roleRows=items<R>(roles.data); const groupRows=items<R>(groups.data); const idpRows=items<R>(idps.data); const oauthRows=items<R>(oauth.data); const identityRows=items<R>(identities.data);
  return <main><PageHeader eyebrow="Tenant security" title="Access" description="People, roles, groups, identity providers and machine identities for this tenant." actions={<LinkButton variant="primary" href={tenantHref(tenantId,"administration")}>Manage access</LinkButton>}/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><ResourceSummary title="Members" description="Users with tenant access." href={tenantHref(tenantId,"administration")} count={memberRows.length} icon={<UsersIcon/>} loading={memberships.loading} error={memberships.error}/><ResourceSummary title="Invitations" description="Pending access invitations." href={tenantHref(tenantId,"administration")} count={inviteRows.length} icon={<ActivityIcon/>} loading={invitations.loading} error={invitations.error}/><ResourceSummary title="Identity providers" description="Tenant federation and SSO." href={tenantHref(tenantId,"administration")} count={idpRows.length} icon={<KeyIcon/>} loading={idps.loading} error={idps.error}/><ResourceSummary title="Machine identities" description="OAuth apps and service identities." href={tenantHref(tenantId,"credentials")} count={oauthRows.length+identityRows.length} icon={<ServerIcon/>} loading={oauth.loading||identities.loading} error={oauth.error||identities.error}/></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><Card className="overflow-hidden"><div className="border-b border-[#E1E7F0] px-5 py-4"><h2 className="font-semibold">Memberships</h2><p className="mt-0.5 text-xs text-[#718096]">Existing human access to this tenant.</p></div><DataTable loading={memberships.loading} columns={[{key:"member",label:"Member"},{key:"status",label:"Status"},{key:"roles",label:"Roles"}]} rows={memberRows.map(m=>({key:idOf(m),cells:{member:<strong>{text(m.displayName,text(m.email,text((m.user as R|undefined)?.email,"Member")))}</strong>,status:<StatusText tone={statusTone(m.status)}>{text(m.status,"Active")}</StatusText>,roles:Array.isArray(m.roles)?(m.roles as R[]).map(r=>text(r.name)).join(", "):text(m.roleName,"—")}}))} empty={<EmptyState icon={<UsersIcon/>} title="No memberships" description="Invite a user to grant tenant access."/>}/></Card><div className="space-y-6"><Card className="p-5"><h2 className="font-semibold">Access model</h2><dl className="mt-4 grid grid-cols-2 gap-4 text-sm"><div><dt className="text-[#718096]">Roles</dt><dd className="mt-1 text-xl font-semibold">{roleRows.length}</dd></div><div><dt className="text-[#718096]">Groups</dt><dd className="mt-1 text-xl font-semibold">{groupRows.length}</dd></div><div><dt className="text-[#718096]">OAuth apps</dt><dd className="mt-1 text-xl font-semibold">{oauthRows.length}</dd></div><div><dt className="text-[#718096]">Service identities</dt><dd className="mt-1 text-xl font-semibold">{identityRows.length}</dd></div></dl><div className="mt-5 flex flex-wrap gap-2"><LinkButton className="h-8 px-3 text-xs" href={tenantHref(tenantId,"administration")}>Roles & groups</LinkButton><LinkButton className="h-8 px-3 text-xs" href={tenantHref(tenantId,"credentials")}>Machine credentials</LinkButton></div></Card><Card className="p-5"><h2 className="font-semibold">Federation</h2><p className="mt-1 text-sm text-[#5B6678]">{idpRows.length?`${idpRows.length} identity provider${idpRows.length===1?"":"s"} configured.`:"No tenant identity providers configured."}</p><LinkButton className="mt-4 h-8 px-3 text-xs" href={tenantHref(tenantId,"administration")}>Manage identity providers</LinkButton></Card></div></div></main>;
}

export function TenantActivity({tenantId}:{tenantId:string}) {
  const root=`/api/tenants/${encodeURIComponent(tenantId)}`; const operations=useApi<unknown>(`${root}/operations`); const audit=useApi<unknown>(`${root}/audit-log`); const opRows=items<R>(operations.data); const auditRows=items<R>(audit.data);
  return <main><PageHeader eyebrow="Tenant history" title="Activity" description="Operations and audit history for changes made in this tenant." actions={<div className="flex gap-2"><LinkButton href={tenantHref(tenantId,"operations")}>Operations</LinkButton><LinkButton href={tenantHref(tenantId,"audit")}>Audit log</LinkButton></div>}/><div className="grid gap-6 xl:grid-cols-2"><Card className="overflow-hidden"><div className="flex items-center gap-2 border-b border-[#E1E7F0] px-5 py-4"><ActivityIcon className="text-[#1769E0]"/><h2 className="font-semibold">Operations</h2></div><DataTable loading={operations.loading} columns={[{key:"operation",label:"Operation"},{key:"status",label:"Status"},{key:"time",label:"Timestamp"}]} rows={opRows.slice(0,12).map(o=>({key:idOf(o),href:tenantHref(tenantId,"operations",idOf(o)),cells:{operation:<strong>{text(o.type,"Operation").replaceAll("_"," ")}</strong>,status:<StatusText tone={statusTone(o.status)}>{text(o.status,"Unknown")}</StatusText>,time:formatDate(o.createdAt??o.updatedAt)}}))} empty={<EmptyState icon={<ActivityIcon/>} title="No operations" description="Runtime and deployment jobs will appear here."/>}/></Card><Card className="overflow-hidden"><div className="flex items-center gap-2 border-b border-[#E1E7F0] px-5 py-4"><GridIcon className="text-[#1769E0]"/><h2 className="font-semibold">Audit log</h2></div><DataTable loading={audit.loading} columns={[{key:"action",label:"Action"},{key:"actor",label:"Actor"},{key:"time",label:"Timestamp"}]} rows={auditRows.slice(0,12).map(a=>({key:idOf(a)||`${text(a.action)}-${text(a.createdAt)}`,cells:{action:<strong>{text(a.action,"Change")}</strong>,actor:text(a.actorDisplayName,text(a.actorEmail,text(a.actor,"System"))),time:formatDate(a.createdAt)}}))} empty={<EmptyState icon={<GridIcon/>} title="No audit records" description="Security and configuration changes will appear here."/>}/></Card></div></main>;
}

export function TenantBilling({tenantId}:{tenantId:string}) {
  const root=`/api/tenants/${encodeURIComponent(tenantId)}`;
  const billing=useApi<R>(`${root}/billing`); const quota=useApi<R|undefined>(`${root}/quota`); const transactions=useApi<unknown>(`${root}/billing/transactions`); const usage=useApi<unknown>(`${root}/billing/usage-records`);
  const [quotaForm,setQuotaForm]=useState({cpu:0,memoryBytes:0,gpu:0,storageBytes:0,maxSingleApps:0,maxVolumes:0}); const [topup,setTopup]=useState({amount:"",reference:""}); const [working,setWorking]=useState<string>(); const [error,setError]=useState<string>(); const [success,setSuccess]=useState<string>();
  useEffect(()=>{if(!quota.data)return;setQuotaForm({cpu:Number(quota.data.cpu??0),memoryBytes:Number(quota.data.memoryBytes??0),gpu:Number(quota.data.gpu??0),storageBytes:Number(quota.data.storageBytes??0),maxSingleApps:Number(quota.data.maxSingleApps??0),maxVolumes:Number(quota.data.maxVolumes??0)});},[quota.data]);
  async function saveQuota(){setWorking("quota");setError(undefined);setSuccess(undefined);try{await apiRequest(`${root}/quota`,{method:"PATCH",body:quotaForm});setSuccess("Quota saved.");await quota.reload();}catch(c){setError(c instanceof Error?c.message:"Quota could not be saved.");}finally{setWorking(undefined);}}
  async function addBalance(){if(!topup.amount)return;setWorking("topup");setError(undefined);setSuccess(undefined);try{await apiRequest(`${root}/billing/top-up`,{method:"POST",body:{amount:topup.amount,reference:topup.reference||undefined}});setSuccess("Balance updated.");setTopup({amount:"",reference:""});await Promise.all([billing.reload(),transactions.reload()]);}catch(c){setError(c instanceof Error?c.message:"Top-up failed.");}finally{setWorking(undefined);}}
  const txRows=items<R>(transactions.data); const usageRows=items<R>(usage.data); const state=text(billing.data?.billingState,text(billing.data?.state,"Unknown"));
  return <main><PageHeader eyebrow="Tenant finance" title="Billing" description="Balance, quota, usage and account transactions."/>{error?<div className="mb-4"><Callout tone="danger" title="Billing request failed">{error}</Callout></div>:null}{success?<div className="mb-4"><Callout tone="success" title="Billing updated">{success}</Callout></div>:null}<div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Card className="p-5"><div className="flex items-start justify-between"><span className="flex h-10 w-10 items-center justify-center rounded-lg bg-[#E7F1FF] text-[#1769E0]"><BillingIcon/></span><StatusBadge tone={statusTone(state)}>{state}</StatusBadge></div><p className="mt-4 text-xs font-medium text-[#718096]">Balance</p><strong className="mt-1 block text-2xl">{text(billing.data?.balanceCredits,text(billing.data?.balance,"0"))} credits</strong><span className="text-xs text-[#718096]">≈ {text(billing.data?.balancePln,"0")} PLN</span></Card><Card className="p-5"><p className="text-xs font-medium text-[#718096]">CPU quota</p><strong className="mt-2 block text-2xl">{text(quota.data?.cpu,"—")}</strong><span className="text-xs text-[#718096]">cores</span></Card><Card className="p-5"><p className="text-xs font-medium text-[#718096]">Memory quota</p><strong className="mt-2 block text-2xl">{formatBytes(quota.data?.memoryBytes)}</strong></Card><Card className="p-5"><p className="text-xs font-medium text-[#718096]">Storage quota</p><strong className="mt-2 block text-2xl">{formatBytes(quota.data?.storageBytes)}</strong></Card></div><div className="mt-6 grid gap-6 xl:grid-cols-[1fr_.8fr]"><Card className="p-5"><h2 className="font-semibold">Quota</h2><p className="mt-1 text-sm text-[#5B6678]">Resource limits applied to this tenant.</p><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-3"><Field label="CPU cores"><NumberInput min={0} step={0.1} value={quotaForm.cpu} onChange={e=>setQuotaForm({...quotaForm,cpu:Number(e.target.value)})}/></Field><Field label="Memory (bytes)"><NumberInput min={0} value={quotaForm.memoryBytes} onChange={e=>setQuotaForm({...quotaForm,memoryBytes:Number(e.target.value)})}/></Field><Field label="GPU"><NumberInput min={0} value={quotaForm.gpu} onChange={e=>setQuotaForm({...quotaForm,gpu:Number(e.target.value)})}/></Field><Field label="Storage (bytes)"><NumberInput min={0} value={quotaForm.storageBytes} onChange={e=>setQuotaForm({...quotaForm,storageBytes:Number(e.target.value)})}/></Field><Field label="Max applications"><NumberInput min={0} value={quotaForm.maxSingleApps} onChange={e=>setQuotaForm({...quotaForm,maxSingleApps:Number(e.target.value)})}/></Field><Field label="Max volumes"><NumberInput min={0} value={quotaForm.maxVolumes} onChange={e=>setQuotaForm({...quotaForm,maxVolumes:Number(e.target.value)})}/></Field></div><Button className="mt-5" variant="primary" disabled={working==="quota"} onClick={()=>void saveQuota()}>{working==="quota"?"Saving…":"Save quota"}</Button></Card><Card className="p-5"><h2 className="font-semibold">Top up balance</h2><p className="mt-1 text-sm text-[#5B6678]">Add credits using an approved top-up or voucher reference.</p><div className="mt-5 space-y-4"><Field label="Amount" required><TextInput inputMode="decimal" value={topup.amount} onChange={e=>setTopup({...topup,amount:e.target.value})} placeholder="100"/></Field><Field label="Reference"><TextInput value={topup.reference} onChange={e=>setTopup({...topup,reference:e.target.value})} placeholder="Optional reference"/></Field><Button variant="primary" disabled={working==="topup"||!topup.amount} onClick={()=>void addBalance()}>{working==="topup"?"Updating…":"Top up"}</Button></div></Card></div><div className="mt-6 grid gap-6 xl:grid-cols-2"><Card className="overflow-hidden"><div className="border-b border-[#E1E7F0] px-5 py-4"><h2 className="font-semibold">Transactions</h2></div><DataTable loading={transactions.loading} columns={[{key:"type",label:"Type"},{key:"amount",label:"Amount"},{key:"time",label:"Timestamp"}]} rows={txRows.slice(0,20).map(t=>({key:idOf(t)||`${text(t.type)}-${text(t.createdAt)}`,cells:{type:<strong>{text(t.type,"Transaction")}</strong>,amount:text(t.amount,text(t.credits,"—")),time:formatDate(t.createdAt)}}))} empty={<EmptyState icon={<BillingIcon/>} title="No transactions" description="Billing transactions will appear here."/>}/></Card><Card className="overflow-hidden"><div className="border-b border-[#E1E7F0] px-5 py-4"><h2 className="font-semibold">Usage</h2></div><DataTable loading={usage.loading} columns={[{key:"resource",label:"Resource"},{key:"quantity",label:"Usage"},{key:"time",label:"Timestamp"}]} rows={usageRows.slice(0,20).map(u=>({key:idOf(u)||`${text(u.resourceType)}-${text(u.createdAt)}`,cells:{resource:<strong>{text(u.resourceType,text(u.metric,"Usage"))}</strong>,quantity:text(u.quantity,text(u.amount,"—")),time:formatDate(u.createdAt??u.periodStart)}}))} empty={<EmptyState icon={<ActivityIcon/>} title="No usage records" description="Usage records will appear after resources consume billable capacity."/>}/></Card></div></main>;
}
